services:
  - type: web
    name: misinformation-ui
    env: python
    runtime: python3.11
    buildCommand: pip install -r requirements.txt
    startCommand: cd app && uvicorn main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
    build:
      env:
        PYTHON_VERSION: 3.11.14